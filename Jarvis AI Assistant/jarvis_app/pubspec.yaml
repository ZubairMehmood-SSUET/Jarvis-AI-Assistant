name: jarvis_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  google_sign_in: ^7.2.0
  
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  camera: ^0.10.6 # Sirf camera package rakha hai
  web: ^1.1.1
  file_picker: ^11.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true